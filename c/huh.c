#include <stdio.h>

int main() {
    int y = 2 / 4;

    if (y == 0) {
        printf("I was told that in maths if the number was larget than .5 it would round UP not DOWN wtf C???");
    }

    return 0;
}